const Loading = () => {
  return (
    <div>
      <img
        src={`/bouncing-ball.svg`}
        alt="Chargement en cours"
        className="img loading"
      />
    </div>
  );
};

export default Loading;
